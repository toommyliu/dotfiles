{
  "profiles": [
    {
      "complex_modifications": {
        "rules": [
          {
            "description": "Caps Lock → Hyper Key (⌃⌥⇧⌘) / Caps Lock if alone",
            "manipulators": [
              {
                "type": "basic",
                "from": {
                  "key_code": "caps_lock",
                  "modifiers": { "optional": ["any"] }
                },
                "to": [
                  {
                    "key_code": "left_shift",
                    "modifiers": [
                      "left_command",
                      "left_control",
                      "left_option"
                    ]
                  }
                ],
                "": "Press and release under 200ms → treated as a tap → to_if_alone fires → caps lock toggles",
                "to_if_alone": [
                  {
                    "key_code": "caps_lock",
                    "hold_down_milliseconds": 100
                  }
                ],
                "": "Press and hold over 200ms → treated as a hold → to fires → hyper key activates",
                "parameters": {
                  "basic.to_if_alone_timeout_milliseconds": 200
                }
              }
            ]
          }
        ]
      },
      "name": "Default profile",
      "selected": true,
      "virtual_hid_keyboard": { "keyboard_type_v2": "ansi" }
    }
  ]
}